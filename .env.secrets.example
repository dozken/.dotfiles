# Copy to ~/.env.secrets and fill in. Sourced by ~/.zshrc if present.
# This file is a template — NEVER put real secrets here (it is tracked in git).
# ~/.env.secrets itself is NOT tracked.
#
# export OPENAI_API_KEY="..."
# export ANTHROPIC_API_KEY="..."
# export GITHUB_TOKEN="..."
